{
  "name": "init-node",
  "description": "Initialize repository, ci, and coverage for node packages.",
  "repository": "git://github.com/joshrtay/init-node.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/log-error": "^1.0.0",
    "prosh": "^1.2.4"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
